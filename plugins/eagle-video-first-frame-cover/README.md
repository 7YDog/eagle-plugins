# 切换视频首帧封面

从 Eagle 中选择一个或多个视频，提取第 0 秒的首帧画面，并将其设置为视频的自定义缩略图。

## 功能

- 支持同时处理多个视频。
- 支持 MP4、WebM、AVI、MOV、MKV、FLV、WMV 和 M4V。
- 通过 Eagle 的 FFmpeg 扩展提取画面，无需在插件中内置 FFmpeg。
- 显示处理进度和运行日志。

## 使用方法

1. 在 Eagle 中选择需要处理的视频。
2. 打开“切换视频首帧封面”插件。
3. 点击“一键提取并设为封面”。

## 版本

当前版本：1.0.1

依赖：Eagle FFmpeg 扩展。
